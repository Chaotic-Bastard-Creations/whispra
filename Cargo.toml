[package]
name = "whispra"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["custom-protocol"] }
axum = {version = "0.8.4", features=["ws"]}
tokio = {version = "1", features = ["full"]}
anyhow = "1.0.102"
aes-gcm = "0.10.3"
